'use strict';

const {
  concat
} = require('./concat');

const {
  every
} = require('./every');

const {
  fill
} = require('./fill');

const {
  filter
} = require('./filter');

const {
  find
} = require('./find');

const {
  findIndex
} = require('./findIndex');

const {
  forEach
} = require('./forEach');

const {
  from
} = require('./from');

const {
  getData
} = require('./connection');

const {
  includes
} = require('./includes');

const {
  indexOf
} = require('./indexOf');

const {
  isArray
} = require('./isArray');

const {
  length
} = require('./length');

const {
  of
} = require('./of');

module.exports = {
  concat,
  every,
  fill,
  filter,
  find,
  findIndex,
  forEach,
  from,
  getData,
  includes,
  indexOf,
  isArray,
  length,
  of
};
