'use strict';

const {
  concat
} = require('./concat');

const {
  every
} = require('./every');

const {
  fill
} = require('./fill');

const {
  from
} = require('./from');

const {
  getData
} = require('./connection');

const {
  isArray
} = require('./isArray');

const {
  length
} = require('./length');

const {
  of
} = require('./of');

module.exports = {
  concat,
  every,
  fill,
  from,
  getData,
  isArray,
  length,
  of
};
