'use strict';

const {
  from
} = require('./from');

const {
  getData
} = require('./connection');

const {
  isArray
} = require('./isArray');

const {
  length
} = require('./length');

const {
  of
} = require('./of');

module.exports = {
  from,
  getData,
  isArray,
  length,
  of
};
