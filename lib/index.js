'use strict';

const {
  concat
} = require('./concat');

const {
  every
} = require('./every');

const {
  fill
} = require('./fill');

const {
  filter
} = require('./filter');

const {
  find
} = require('./find');

const {
  findIndex
} = require('./findIndex');

const {
  forEach
} = require('./forEach');

const {
  from
} = require('./from');

const {
  getData
} = require('./connection');

const {
  includes
} = require('./includes');

const {
  indexOf
} = require('./indexOf');

const {
  isArray
} = require('./isArray');

const {
  join
} = require('./join');

const {
  keys
} = require('./keys');

const {
  lastIndexOf
} = require('./lastIndexOf');

const {
  length
} = require('./length');

const {
  map
} = require('./map');

const {
  of
} = require('./of');

const {
  pop
} = require('./pop');

const {
  push
} = require('./push');

const {
  reduce
} = require('./reduce');

module.exports = {
  concat,
  every,
  fill,
  filter,
  find,
  findIndex,
  forEach,
  from,
  getData,
  includes,
  indexOf,
  isArray,
  join,
  keys,
  lastIndexOf,
  length,
  map,
  of,
  pop,
  push,
  reduce
};
