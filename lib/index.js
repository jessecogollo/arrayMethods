'use strict';

const {
  concat
} = require('./concat');

const {
  every
} = require('./every');

const {
  fill
} = require('./fill');

const {
  filter
} = require('./filter');

const {
  from
} = require('./from');

const {
  getData
} = require('./connection');

const {
  isArray
} = require('./isArray');

const {
  length
} = require('./length');

const {
  of
} = require('./of');

module.exports = {
  concat,
  every,
  fill,
  filter,
  from,
  getData,
  isArray,
  length,
  of
};
